# Graphql Schema directory

Django will output scheam.json files into this directory when you run the command `python manage.py graphql_schema`.
This file can be consumed by Relay by running `npm run relay`.
